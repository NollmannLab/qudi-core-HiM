experiment: sequential_injections
description: Configure a fluidics experiment consisting of buffer injections, probe injections, and incubation steps.

output_filename: fluidics_task.yaml

sections:
  documents: true

fields:
  injections_path:
    required: true
    default: ""
